<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pipe System Manager" Height="550" Width="900"
        WindowStartupLocation="Manual"
        ResizeMode="CanResize"
        Topmost="True"
        WindowStyle="None"
        BorderBrush="#0080FF"
        BorderThickness="1"
        Background="#FAFAFA">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#CCE8FF"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#F0F0F0"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black"/>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- List -->
            <RowDefinition Height="Auto"/> <!-- Actions -->
            <RowDefinition Height="Auto"/> <!-- Status -->
        </Grid.RowDefinitions>

        <!-- Header (Custom Title Bar) -->
        <Border Name="HeaderDrag" Grid.Row="0" Background="#0080FF" Padding="15,10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Pipe System Manager" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Visualize, Rename &amp; Merge Networks" FontSize="11" Foreground="#E0E0E0"/>
                </StackPanel>

                <Button Name="Btn_WinClose" Grid.Column="1" Content="✕" Width="30" Height="30" 
                        Foreground="White" Background="Transparent" BorderThickness="0" 
                        FontSize="14" FontWeight="Bold" Cursor="Hand" VerticalAlignment="Top" HorizontalAlignment="Right">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#FFCCCC"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="15,0,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <Button Name="Btn_ScanView" Content="Scan Active View" FontWeight="SemiBold" Background="#E6F7FF" BorderBrush="#1890FF" ToolTip="Find all pipe networks in the current view."/>
                <Button Name="Btn_SelectAll" Content="Select All" Margin="10,0,0,0" ToolTip="Check all systems in the list."/>
                <Button Name="Btn_Clear" Content="Clear List" ToolTip="Clear the list and reset view." Margin="10,0,0,0"/>
                <CheckBox Name="Cb_AutoZoom" Content="Auto-Zoom" VerticalAlignment="Center" Margin="10,0,0,0" ToolTip="Automatically zoom view to fit selected system."/>
            </StackPanel>
        </Grid>

        <!-- Tree View Header -->
        <Grid Grid.Row="2" Margin="15,0,15,0" VerticalAlignment="Top" Height="25" Background="#F5F5F5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/> <!-- System Browser -->
                <ColumnDefinition Width="80"/>  <!-- Abbr -->
                <ColumnDefinition Width="60"/>  <!-- FU -->
                <ColumnDefinition Width="90"/>  <!-- Length -->
                <ColumnDefinition Width="90"/>  <!-- Volume -->
                <ColumnDefinition Width="*"/>   <!-- Count -->
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="5,0"><TextBlock Text="System Browser" FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/></Border>
            <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="5,0"><TextBlock Text="Abbreviation" FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/></Border>
            <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="5,0"><TextBlock Text="FU" FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/></Border>
            <Border Grid.Column="3" BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="5,0"><TextBlock Text="Total Length" FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/></Border>
            <Border Grid.Column="4" BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="5,0"><TextBlock Text="Vol (ft³)" FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/></Border>
            <Border Grid.Column="5" Padding="5,0"><TextBlock Text="Networks" FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/></Border>
        </Grid>

        <!-- Tree View -->
        <TreeView Name="systemTree" Grid.Row="2" Margin="15,25,15,15" BorderBrush="#DDDDDD" BorderThickness="1">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="380"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" BorderBrush="#F0F0F0" BorderThickness="0,0,1,0" Padding="2,4,5,4">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsChecked}" VerticalAlignment="Center" Margin="0,0,5,0" Click="on_checkbox_click"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="{Binding FontWeight}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="1" BorderBrush="#F0F0F0" BorderThickness="0,0,1,0" Padding="5,4">
                                <TextBlock Text="{Binding Abbreviation}" VerticalAlignment="Center" Foreground="#333"/>
                            </Border>
                            <Border Grid.Column="2" BorderBrush="#F0F0F0" BorderThickness="0,0,1,0" Padding="5,4">
                                <TextBlock Text="{Binding FixtureUnits}" VerticalAlignment="Center"/>
                            </Border>
                            <Border Grid.Column="3" BorderBrush="#F0F0F0" BorderThickness="0,0,1,0" Padding="5,4">
                                <TextBlock Text="{Binding Length}" VerticalAlignment="Center"/>
                            </Border>
                            <Border Grid.Column="4" BorderBrush="#F0F0F0" BorderThickness="0,0,1,0" Padding="5,4">
                                <TextBlock Text="{Binding Volume}" VerticalAlignment="Center"/>
                            </Border>
                            <Border Grid.Column="5" Padding="5,4">
                                <TextBlock Text="{Binding Count}" VerticalAlignment="Center" Foreground="{Binding NetworkColor}"/>
                            </Border>
                        </Grid>
                    </Border>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- Actions -->
        <Grid Grid.Row="3" Margin="15,0,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Color Selection -->
            <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Color:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Name="Cmb_Colors" Width="140" Height="26" VerticalContentAlignment="Center">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" Background="{Binding Brush}" BorderBrush="Gray" BorderThickness="1" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <!-- Visualization & Merge -->
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <Button Name="Btn_Visualize" Content="🎨 Colorize Checked" ToolTip="Apply selected color to checked systems."/>
                <Button Name="Btn_ClearVisuals" Content="🚫 Reset Colors" ToolTip="Remove color overrides from checked systems."/>
                <Border Width="1" Background="#DDD" Margin="5,2,10,2"/>
                <Button Name="Btn_Disconnect" Content="🔌 Disconnect" FontWeight="SemiBold" ToolTip="Disconnect selected systems from fixtures/equipment to allow regrouping."/>
            </StackPanel>

            <!-- Rename -->
            <StackPanel Orientation="Horizontal" Grid.Column="2">
                <TextBlock Text="Rename:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray"/>
                <TextBox Name="Tb_NewName" Width="100" VerticalContentAlignment="Center" Margin="0,0,5,0" Padding="2"/>
                <Button Name="Btn_Rename" Content="Apply" Background="#F0F0F0"/>
            </StackPanel>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="4" Background="#F0F0F0" Padding="10,5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <TextBlock Name="statusLabel" Text="Ready. Select pipes to begin." Foreground="#666" FontSize="11"/>
        </Border>
    </Grid>
</Window>